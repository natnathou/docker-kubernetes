{
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "watch": "^1.0.2"
  },
  "scripts": {
    "build": "npm-run-all --sequential build:**",
    "build:one": "docker build -t natnathou/dockerproject -f Dockerfile.dev .",
    "build:two": "echo Delete old container...",
    "build:three": "docker rm -f containenotreprojet",
    "build:four": "echo Run new container... ",
    "build:five": "docker run -it -p 3000:3000 -v /app/node_modules -v \"/$(pwd)/frontend\":/app  --name containenotreprojet natnathou/dockerproject"
  }
}
